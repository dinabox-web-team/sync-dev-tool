---
title: Quick Start
---

## 🚀 Quick Start

Baixe a versão mais recente e estável (branch `main`) e instale:

```bash
# usando curl
curl -L https://raw.githubusercontent.com/dinabox-web-team/sync-dev-tool/main/sync-dev.sh -o sync-dev.sh

# instalar (requer sudo)
sudo bash sync-dev.sh --install

# após instalar (disponível globalmente)
sync-dev --help
```

Alternativa com wget:

```bash
wget -O sync-dev.sh https://raw.githubusercontent.com/dinabox-web-team/sync-dev-tool/main/sync-dev.sh
sudo bash sync-dev.sh --install
```

> Segurança: prefira baixar de `main` (branch estável) ou de Releases assinadas. Veja a seção de segurança para opções de verificação.
