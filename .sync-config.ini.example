# ===============================================
# Arquivo de Configuração do sync-dev - TEMPLATE
# ===============================================
# Este é um template. Copie para .sync-config.ini e preencha com seus dados.
# NUNCA commite o arquivo .sync-config.ini (ele contém credenciais!)

# ============ OBRIGATÓRIAS ============

# Host do servidor remoto (ex: server.example.com)
HOST=

# Caminho completo no servidor remoto onde os arquivos serão sincronizados
# Exemplo: /var/www/html ou /opt/app
REMOTE_PATH=

# Usuário SSH para conexão
USER=

# Grupo para aplicar nos arquivos remotos
# Exemplo: www-data, apache, nginx, adm
GROUP=

# Caminho completo para a chave SSH privada
# Exemplo: /home/user/.ssh/id_rsa
SSH_KEY=

# ============ OPCIONAIS ============

# Diretório local (padrão: diretório atual onde o script é executado)
# Descomente e defina se quiser um caminho específico
# LOCAL_PATH=/home/user/projeto

# Permissões para diretórios no servidor remoto (padrão: 775)
# 775 = rwxrwxr-x (dono: rwx, grupo: rwx, outros: r-x)
CHMOD_DIRS=775

# Permissões para arquivos no servidor remoto (padrão: 664)
# 664 = rw-rw-r-- (dono: rw-, grupo: rw-, outros: r--)
CHMOD_FILES=664

# Permissões ao sair - diretórios (padrão: 775)
CHMOD_DIRS_EXIT=775

# Permissões ao sair - arquivos (padrão: 664)
CHMOD_FILES_EXIT=664

# Tempo de debounce em segundos para sincronização automática (padrão: 2)
# Aumente se tiver muitas mudanças rápidas (ex: compilação)
DEBOUNCE_TIME=2

# Arquivos e diretórios a ignorar (separados por vírgula, SEM ESPAÇOS)
# Adicione mais padrões conforme necessário
IGNORE_FILES=.git,node_modules,dist,*.log,vendor,tmp,composer.lock,ssh_key,.gitignore,.DS_Store,logs

# ===============================================
# EXEMPLOS DE USO
# ===============================================

# Exemplo 1: Desenvolvimento WordPress
# HOST=server.example.com
# REMOTE_PATH=/var/www/html/wp-content/themes/meu-tema
# USER=developer
# GROUP=www-data
# SSH_KEY=/home/user/.ssh/id_rsa
# IGNORE_FILES=.git,node_modules,*.log,.DS_Store

# Exemplo 2: Desenvolvimento PHP/Laravel
# HOST=staging.myapp.com
# REMOTE_PATH=/var/www/myapp
# USER=deploy
# GROUP=www-data
# SSH_KEY=/home/user/.ssh/deploy_key
# IGNORE_FILES=.git,vendor,node_modules,storage/logs,*.log,.env

# Exemplo 3: Desenvolvimento Node.js
# HOST=dev.nodeapp.com
# REMOTE_PATH=/opt/nodeapp
# USER=nodejs
# GROUP=nodejs
# SSH_KEY=/home/user/.ssh/id_rsa
# IGNORE_FILES=.git,node_modules,dist,build,*.log,.env

# ===============================================
# PERMISSÕES COMUNS
# ===============================================

# Web servers (Apache/Nginx):
# CHMOD_DIRS=755 (rwxr-xr-x)
# CHMOD_FILES=644 (rw-r--r--)

# Desenvolvimento colaborativo:
# CHMOD_DIRS=775 (rwxrwxr-x)
# CHMOD_FILES=664 (rw-rw-r--)

# Mais restritivo:
# CHMOD_DIRS=750 (rwxr-x---)
# CHMOD_FILES=640 (rw-r-----)
